# AGENTS.md

本文件约束在本目录及其子目录中工作的开发者和编码代理。若更深层目录存在自己的 `AGENTS.md`，以更具体的约束为准。

## 项目原则

- 先保证规则、状态和计算正确，再优化解释体验。
- Rust 是确定性事实的来源；LLM 只负责编排、综合与表达。
- Mortal 的输出属于模型建议，不能替代规则校验或确定性算法。
- 每条用户可见结论应能追溯到计算、模型、统计、资料或明确标注的推断。
- 规则变体必须显式建模，禁止把某个平台或赛事规则静默当作通用规则。
- 原始牌谱应保持不可变；解析结果、特征和模型输出必须带版本信息。
- 外部书籍与牌谱数据必须尊重版权、授权、隐私和平台条款。

## 开发方式

在修改代码前：

1. 阅读根目录 `README.md`，确认改动属于哪一层；
2. 搜索现有领域类型、错误类型和测试夹具，避免重复定义；
3. 如果需求涉及未确定的规则变体，在代码和测试中写明假设；
4. 不因顺手重构而扩大改动范围。

提交实现时：

- 保持模块边界清晰，领域核心不得依赖 GUI、具体 LLM SDK 或数据库实现；
- 优先使用强类型表达牌、动作、座位、轮次、分数和规则；
- 不用无约束字符串在核心层传递领域状态；
- 对外部输入执行校验，并提供包含上下文的错误；
- 禁止在日志、夹具或提交内容中加入 API 密钥、私人牌谱或其他敏感信息；
- 新增第三方依赖前说明用途，优先选择维护良好且许可证兼容的依赖；
- 不把大型模型、完整牌谱数据集、OCR 原文或生成产物提交到 Git。

## 架构边界

### 领域与算法

- 领域类型和规则逻辑必须独立于 IO。
- 同一输入在同一规则与版本下应产生稳定结果。
- 算法应返回结构化证据，例如候选动作、有效牌、剩余枚数和计算参数，而非只返回解释文本。
- 对性能优化先保留正确的参考实现和回归测试，再加入缓存、并行或位运算优化。

### 牌谱解析

- 将来源格式与内部规范模型隔离；来源特有字段不得泄漏到通用领域层。
- 保留原始事件位置，以便错误报告和结果追溯。
- 解析过程区分语法错误、规则不兼容、状态不一致和暂不支持的事件。
- 每修复一个解析缺陷，增加最小化回归夹具。

### Mortal 与其他模型

- 通过 trait 或明确的端口类型接入，避免核心代码绑定具体进程或传输协议。
- 保存模型版本、输入模式、推理参数和原始响应。
- 对超时、不可用、无效响应和版本不兼容提供显式错误或降级结果。
- 测试默认使用固定响应的 fake；普通单元测试不得依赖网络或本地模型服务。

### LLM 与知识检索

- LLM 工具使用版本化、可序列化的输入输出 schema。
- 不让 LLM 生成或修补权威局面状态；状态变更必须经过 Rust 校验。
- 检索结果保留文献标识、位置和授权元数据，回答中的引用必须能定位原文。
- 知识库内容视为不可信输入，防止其文本覆盖系统规则或触发未授权工具调用。
- 测试中使用 fake provider；不得要求开发者拥有真实云端密钥才能运行默认测试。

### 数据与训练

- 统计事实、训练标签和模型预测使用不同字段或表，禁止混用。
- 数据集必须记录来源范围、规则过滤、特征版本、标签版本和切分方法。
- 训练集、验证集和测试集按玩家或对局进行合理隔离，避免相邻局面泄漏。
- 任何模型优化都要与简单基线在固定评估集上比较。

## 测试要求

- 新功能和缺陷修复必须配套测试。
- 规则与算法优先使用表驱动测试和属性测试；边界至少覆盖副露、立直、杠、流局、终局和规则变体中适用的情况。
- 解析器应覆盖正常样本、截断输入、未知事件和非法状态转换。
- 快照测试只用于结构稳定且人工可审查的输出；不要用大快照掩盖断言缺失。
- 随机测试必须固定或输出种子，使失败可复现。

完成 Rust 改动后，至少运行：

```bash
cargo fmt --check
cargo test
cargo clippy --all-targets --all-features -- -D warnings
```

如果某项检查无法运行，应在交付说明中写明原因，不得声称已经通过。

## 文档要求

- 新增牌谱格式、规则变体、工具 schema 或持久化格式时同步更新文档。
- 对难以逆转的技术选择，在 `docs/adr/` 中增加简短的架构决策记录。
- README 只描述已经实现的能力或明确标注为计划的能力。
- 示例数据应最小化、匿名化，并注明来源或生成方式。

## 当前阶段

项目仍处于脚手架阶段。近期改动应围绕 README 中的 MVP 前三步展开。除非任务明确要求，否则不要一次性创建完整 workspace、引入 GUI/LLM 框架或搭建训练基础设施。
