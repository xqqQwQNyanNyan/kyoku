# 在项目根目录将本文件复制为 .env，填写后直接运行 agent。
# 优先级：命令行参数 > 已有环境变量 > .env。

# 中转站：填写完整 Responses 地址、模型名和专用密钥。
KYOKU_OPENAI_ENDPOINT='https://example.com/v1/responses'
OPENAI_MODEL='replace-with-model-name'
AGENT_API_KEY=''

# 使用官方服务时，删除或注释上面的地址和 AGENT_API_KEY，另填：
# OPENAI_API_KEY=''
# 本地无认证服务则删除或注释 AGENT_API_KEY 整行。
# 密钥建议用单引号包裹，避免其中的 $ 被当成变量引用。
